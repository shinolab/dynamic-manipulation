#include "profile.hpp"
#include "odcs.hpp"
#include "arfModel.hpp"
#include <iostream>
#include <algorithm>
#define _USE_MATH_DEFINES
#include <math.h>
int main()
{
	/*
	FloatingObjectPtr objPtr = FloatingObject::Create(Eigen::Vector3f(0, 0, 1000));
	auto controller = std::make_shared<ocs>(); controller->Initialize();

	std::cout << "OCS Initialized." << std::endl;

	Eigen::VectorXf duty_limit(controller->centersAUTD.cols()); duty_limit.setConstant(1.0f);

	profileMaxAccel p(Eigen::Vector3f(0.f, 0.f, 1100.f),
	Eigen::Vector3f(0.f, 0.f, 400.f),
	duty_limit, controller,
	objPtr,
	0.05f);

	std::cout << "PROFILE GENERATED." << std::endl;
	auto itrTime = p.pathTime.begin();
	auto itrPos = p.pathPos.begin();
	auto itrVel = p.pathVel.begin();
	std::cout << "velocity size: " << p.pathVel.size() << std::endl;
	for(; itrTime != p.pathTime.end(); itrTime++, itrPos++, itrVel++){
	std::cout << (*itrTime) << ", " << (*itrPos).x() << ", " << (*itrPos).y() << ", " << (*itrPos).z() << ", "
	<< (*itrVel).x() << ", " << (*itrVel).y() << ", " << (*itrVel).z() << std::endl;
	}

	*/
	

	

}